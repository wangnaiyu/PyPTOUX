{
  "id": "workbench-shell",
  "name": "Workbench Shell",
  "type": "hybrid",
  "description": "Reusable PTO application shell for topbar + resizable three-pane workbenches with borderless rounded gray pane cards and a canvas viewport control group. The shell owns Split.js pane dragging, persisted pane sizes, resize callbacks, and standardized 40%-120% canvas zoom controls.",
  "source": {
    "visualContract": [
      "ascend-950-workbench-demo/index.html",
      "ascend-950-workbench-demo/feature_taxonomy.html",
      "patterns/memory-architecture/pattern.js",
      "tokens/components.css workbench-frame and inspector variables"
    ],
    "patternHtml": "patterns/workbench-shell/pattern.html",
    "patternCss": "patterns/workbench-shell/pattern.css",
    "renderScript": "patterns/workbench-shell/pattern.js",
    "thirdParty": "No runtime dependency required. Split.js 1.6.5 is supported if a product page already vendors it; pointer-event fallback is built into pattern.js."
  },
  "useWhen": [
    "building PTO pages with a fixed topbar and three major work regions",
    "making left source/filter panes, center graph or hardware canvases, and right inspector panes draggable",
    "standardizing canvas zoom controls, detail toggles, and resize-driven canvas refit behavior"
  ],
  "requiredApis": [
    "window.PtoWorkbenchShell.initResizablePanes(options)",
    "window.PtoWorkbenchShell.initCanvasControls(options)",
    "window.PtoWorkbenchShell.initWorkbenchShell(options)",
    "window.PtoWorkbenchShell.getZoomLevels(options)"
  ],
  "allowedOverrides": [
    "pane selectors, initial sizes, min sizes, storage key, and gutter size",
    "canvas zoom range, default zoom, and callbacks for applying zoom to a page-specific renderer",
    "topbar content, pane body content, and inspector section content",
    "outer page height and preview embedding chrome"
  ],
  "forbiddenOverrides": [
    "adding pane borders or flattening the three pane cards into a single black divided surface",
    "reimplementing Split.js gutters and localStorage size persistence in each product page",
    "creating page-local canvas zoom buttons with different min/max/step behavior",
    "styling draggable gutters with one-off colors, large handles, or hidden focus states",
    "scaling only route overlays, tags, or tooltips instead of applying one unified canvas scale contract"
  ],
  "agentReuseRule": "Read this JSON first. For a new topbar + three-pane PTO workbench, load pattern.css and pattern.js, then call initWorkbenchShell with pane selectors plus canvas callbacks. If the page already vendors Split.js, the helper will use it; otherwise the built-in pointer-event splitter is used. Keep page-specific renderers such as memory-architecture inside the center pane; do not duplicate shell dragging or zoom controller code locally."
}
