# 2026-06-24 Toolkit source governance update

## Owner

- owner: Codex
- status: applied
- decision: keep `pypto-toolkit` scoped to Toolkit and `pypto-tools`

## Confirmed Boundaries

- `pypto-tools` mirror is planned under `/Users/wny/Documents/2 领域 Area/工作/EASY CANN/样例工程&文件`; it has not been downloaded or cloned.
- PyPTOUX may record `pypto-tools` file structure, schema, screenshots and small code snippets.
- Raw toolkit design files must stay outside PyPTOUX under `/Users/wny/Documents/2 领域 Area/工作/EASY CANN/设计文件`; thumbnails, screenshots and structure summaries are allowed.
- PyPTO sample data, test data, compile/runtime artifacts, data levels and `share-safe` rules have moved to `02-knowledge/00-shared/pypto-data/`.

## Files Updated

- `overview.md`
- `sources.md`
- `manifest.md`

No external clone, data copy or raw design-file write was performed.

## Product Map Follow-up

- owner: Codex
- status: applied
- decision: treat `pypto-toolkit` as product capability map plus demo interaction baseline, not a full VS Code plugin development manual

### User Confirmed Decisions

- `02-knowledge/00-shared/pypto-toolkit` should support product understanding and demo exploration based on `pypto-tools`.
- The `pypto-tools` run-data model is first-class knowledge and should start inside `overview.md`.
- Code / compute graph / swimlane linkage should be documented as a separate mechanism file under this topic.
- `src/boot-mock-data/` should be marked as `toolkit fixture / schema example`, not as L1 sample data by default.
- VS Code plugin capability entry points should be documented in `feature-map.md`.
- Accuracy report, memory visualization and `perf_swimlane.json` conversion should be included in the feature map.

### Files Updated

- `overview.md`: added run-data model, extended capabilities and fixture ownership rules.
- `feature-map.md`: added VS Code entry points, view routes, core feature map and extension capabilities.
- `linkage-mechanism.md`: added the code-compute-swimlane linkage mechanism and demo implications.
- `sources.md`: added `pypto-tools` code evidence paths.
- `manifest.md`: added `toolkit-boot-fixture` intake rule.

No external clone, fetch, data copy, raw design-file write or prototype build was performed.
