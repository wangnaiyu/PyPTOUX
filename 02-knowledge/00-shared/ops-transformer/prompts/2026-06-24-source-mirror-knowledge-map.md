# Composite Prompt: ops-transformer Mirror And Knowledge Map

在 PyPTOUX 仓库中，将 `ops-transformer` 从 candidate source 升级为本地镜像并建立轻量 workflow knowledge map。

要求：

- 使用 `pypto-knowledge-source` 和 `pyptoux-content-router`。
- 本地 mirror 放在 `/Users/wny/Documents/2 领域 Area/工作/EASY CANN/样例工程&文件/ops-transformer`。
- 记录 branch、commit、commit date、subject 和 tracked worktree 状态。
- 在 `02-knowledge/00-shared/ops-transformer/` 新增 `overview.md`、`sources.md`、`notes/update-2026-06-24.md`。
- `overview.md` 聚焦仓库结构、operator workflow、推荐检索路径和版本边界。
- 不复制全量算子列表；后续 demo 需要时再抽 schema 和样例。
