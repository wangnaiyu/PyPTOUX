# 2026-06-23 PyPTO data source terminology sync

## Owner

- owner: Codex
- status: applied
- decision: align architecture orientation wording with Batch C source taxonomy

## Context

Batch C preflight confirmed that `pypto-runtime-data` should not remain a standalone source instance. PyPTO data materials should be governed through concrete source instances:

- `pypto-sample-dataset`: local active source at `/Users/wny/Documents/2 领域 Area/工作/EASY CANN/样例数据`
- `pypto-testdata`: candidate / manifest source pointing to `https://gitcode.com/zhanghuixin/PTO-TestData`

The source type previously named `runtime-artifact-source` is now `pypto-data-source`.

## Changes

- Updated `overview.md` to refer to toolkit / PyPTO data source governance instead of a generic runtime data lane.
- Updated `sources.md` to refer to `pypto-testdata` and PyPTO data source governance.

No `pypto` mirror refresh, clone, runtime data copy, or design file write was performed.
