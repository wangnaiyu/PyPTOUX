---
owner: Codex
status: done
decision: split PyPTO data governance from pypto-toolkit
date: 2026-06-24
---

# PyPTO Data Update 2026-06-24

## Summary

- 新建 `02-knowledge/00-shared/pypto-data/`，承接 PyPTO 样例数据、测试数据、编译 / 运行产物、数据等级和 `share-safe` 规则。
- `pypto-toolkit` 收窄为 Toolkit 产品能力、`pypto-tools` 和 toolkit 设计输入。
- `pypto-sample-dataset` 和 `pypto-testdata` 继续作为 `pypto-data-source`；新增数据材料应归入具体 source instance。

## Decisions

- `pypto-sample-dataset` may write back manifest、摘要、数据等级和 schema；抽样或脱敏后的数据可进入 PyPTOUX。
- `pypto-testdata` may be cloned under `/Users/wny/Documents/2 领域 Area/工作/EASY CANN/样例工程&文件`; its data level is L1 and its permission / external boundary follows `pypto-sample-dataset`.
- Only sampled / desensitized data or schema-generated L2 data can be marked `share-safe`.

## Follow-Ups

- 若后续要实际 clone `pypto-testdata`，需单独授权外部目录写入和网络访问。
- 具体抽样数据、脱敏数据或 schema-generated L2 写入仓库前，逐项确认 material 的 `share-safe` 状态。
