# Composite Prompt: PTO ISA Mirror And Knowledge Map

在 PyPTOUX 仓库中，将 `pto-isa` 从 candidate source 升级为本地镜像并建立 shared knowledge map。

要求：

- 使用 `pypto-knowledge-source` 和 `pyptoux-content-router`。
- 本地 mirror 放在 `/Users/wny/Documents/2 领域 Area/工作/EASY CANN/样例工程&文件/pto-isa`。
- 记录 branch、commit、commit date、subject 和 tracked worktree 状态。
- 在 `02-knowledge/00-shared/pto-isa/` 新增 `overview.md`、`sources.md`、`notes/update-2026-06-24.md`。
- `overview.md` 只做 orientation hints，不作为唯一事实出口。
- 保留上游 literal，如 `TPUT_ASYNC`、`TGET_ASYNC`、`include/pto/common/pto_instr.hpp`。
- 明确与 `pypto-architecture`、`pypto-data`、`ascend-a5-950-hardware` 的边界。
