# 2026-06-27 更新记录

## 本次变更

- 将 `03-insights` 下的 topic 从 `pypto-issue-2141-observability` 改为 `01-fusion-performance-diagnosis`，避免把长期研究主题绑定到单个 issue 编号，并让 `03-insights` 与 `04-uxdesign` / `05-prototypes` 一样形成编号化专题序列。
- 保留报告正文对 `issue #2141` 的证据边界说明，因为该 issue 仍是本次竞品研究的触发来源和业务问题入口。
- 同步更新 `AGENTS.md` 与 `09-docs/01-conventions/content-routing.md`，明确 `03-insights/`、`04-uxdesign/`、`05-prototypes/` 下的非 `00-shared` topic 默认使用两位编号前缀。
- 同步更新 `09-docs/03-indexes/topics.md` 与 `09-docs/03-indexes/content-map.md`。
- 按项目约定补充本次正式输出的 `notes/` 与 `prompts/`；暂不补 `overview.md` 和 `sources.md`，因为当前 HTML 报告底部已内嵌 sources。

## 命名决策

`01-fusion-performance-diagnosis` 表达的是“融合算子性能诊断”业务场景，比 `pypto-issue-2141-observability` 更适合作为长期 topic：

- 它覆盖慢 task 归因、额外 GM/UB 搬运、等待链路、流水利用率和源码回跳闭环。
- 它能继续承接后续 PRD、竞品补充、交互方案和原型，而不需要每个 issue 单独建 topic。
- 它仍可在正文中追溯到 `issue #2141`，不丢失来源。
- `01-` 编号只表达 `03-insights` 的专题序列入口，不表达优先级。

## 当前产物

- `competitive-analysis.html`：阅读型 HTML 竞品分析报告。
- `assets/`：报告使用的真实产品图与用户上传截图副本。
- `notes/update-2026-06-27.md`：本次结构和命名变更记录。
- `prompts/2026-06-27-fusion-performance-diagnosis-competitive-analysis.md`：可复现本次报告方向的复合 prompt。

## 待确认

- 后续如果该 topic 进入正式设计输出，可在 `04-uxdesign/` 下建立同名或相近业务主题，并把竞品结论作为输入引用。
- 如果要继续沉淀长期来源索引，再补 `sources.md`；当前阶段先以 HTML 报告底部 sources 为准。
