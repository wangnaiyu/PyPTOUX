# 剧本 · A5 PMU Group 2 Diagnosis Loop

> 主角 Lin（性能优化专家）。Trace = `run_dav3510_matmul_2026_05_10`，本次 run 采集了 `PROF_PMU_EVENT_TYPE=2`。
> 与原型一一对应；每个 Scene 都有可点击的 UI 终点。

## Scene 0 · 进入

- 顶栏：`Chip: A5 / dav_3510` · `Group: G2 (active, ●sampled)` · `Mix: OFF`。
- 中央 78 lane 按默认顺序渲染：CPU Ctrl / CPU Sched ×3 / AIC ×24 / AIV ×48 / mte_in / mte_out。
- 任务按 **taskIdentity** 着色：matmul 蓝、vec_softmax 绿、vec_layernorm 紫、vec_elementwise 粉、vec_reduce 青、mte 橙、cpu 灰蓝。
- 右下角 Health pill：`sampled group 2` · `tasks ~700+` · `wraps 24`。

## Scene 1 · 摘要触发注意力

左侧 **Immediate Attention**：

- `clc/total` 总解释度芯片（顶部）
- **Critical lane**：`AIC 7`
- **Critical Wrap · top suspect**：`Wrap 7 wait`，给出 `clc=5097 µs < total=8800 µs, gap=-42%`
- **Dominant counter · Wrap 7**：`mte2_instr_busy`（硬件路径 → MTE2 (GM→L1)）
- **Top-3 Wraps · by severity**：
  - W7 · mte2_instr_busy · **-42%**（wait）
  - W12 · scalar_instr_busy · **-29%**（wait）
  - W19 · cube_instr_busy · **+18%**（overlap）
- **下一次采集建议**：`PROF_PMU_EVENT_TYPE=4` 〔📋 复制〕

## Scene 2 · 点击 critical task

- Lin 点左侧 "查看 critical task"，或直接点中央泳道上 AIC 7 的长 task。
- 右侧 Detail Panel：
  - Headline `matmul_trans_k7`
  - 三个 chip：`total 8800 µs · clc 5097 µs · gap -3703 µs · -42.1%`（gap chip 为红色 is-bad）
  - **Conclusion 卡（红边）**：「Wait suspect — clc=5097 µs < total=8800 µs，gap_ratio=-42.1%。当前 group 的 cycle-like counter 只解释了 task 生命周期的 58%。Dominant 指向 mte2_instr_busy（MTE2 (GM→L1)），但仍有显著未解释时间 — 疑似 uncovered / wait / idle / sync / 未统计 stall。」
  - 提示："开启 Mix 模式可看到同 Wrap C/V peer 的开始/结束/拖尾关系"
  - **Counter breakdown**：8 段组合，按 cycle-like 比例排序。mte2 高亮为 dominant。
  - **事件计数 · 不参与 clc 求和**：icache_req / icache_miss（虚线 swatch，标注 event）
  - **Hardware path · dominant**：MTE2 (GM→L1) 解释段
  - **Peer tasks · 同 Wrap**：aic-7 / aiv-13 / aiv-14 三行（aic-7 高亮蓝色 = self）
  - **6 个 Jump 按钮**：源码 / CALL / Block Graph / 硬件路径 / Peer 视图 / 下次采集

## Scene 3 · 切到 Mix 模式

Lin 点顶栏 **Mix: ON**。

- 泳道立即重排：AI CPU ×4 → Wrap 1..24 的 3-lane 块 → mte_in / mte_out。
- 每个 Wrap 块带左侧旋转 90° 的小 tab（W1..W24）。W7 / W12 / W19 的 tab 描琥珀色边，背景为 wrap-critical tint。
- 任务着色切换为 **group 2 counter composition**：compute 靛、scalar 灰、mte1 青绿、mte2 琥珀、mte3 珊瑚、fixpipe 品红。
- Wrap 7 区域：AIC 7 的长 task 出现尾部红色斜纹（uncovered），右端有红色 `!` badge。
- Wrap 19 区域：AIC 19 的 task 覆盖紫色 overlap 罩 + 右侧延伸的紫色刻度（overflow）+ 紫色 `◇` badge。
- Wrap 12 区域：AIC 12 的 task 红色 `!` badge（scalar-bound wait）。

## Scene 4 · Mix 下的新发现

- Lin 在 Wrap 7 区域看到 AIV 13 / 14 早早结束（其 task 段更短），AIC 7 红色斜纹尾部一直延伸到 end。
- 结论：Wait 不是 C 等 V，而是 C 自身在 V 之后还在做 mte2-bound 拖尾。
- Detail panel 的 Peer tasks 显示 aiv-13 / aiv-14 状态为 `accounted`，AIC 7 状态为 `wait`，end 时间差很大。

## Scene 5 · 一跳定位

逐个点击 Detail panel 的 6 个 jump 按钮：

1. **源码** → modal 显示 `kernels/matmul_trans_k7.cce` 的 mock kernel 代码，第 122 行高亮（hotLine），周边 30 行作为 range 背景。语法高亮：关键字紫 / 类型青 / 字符串绿 / 数字橙 / 注释灰。
2. **CALL** → modal 显示调用链：Model.forward → Attention.compute → AttnFusedMatmul.run → **KernelLaunch[matmul_trans_k7]**（高亮）→ CopyIn / LoadToL0 / Compute / FixOutput 子节点。
3. **Block Graph** → modal 显示 9 节点 SVG。`Compute` 节点描黄色（is-target），相邻 LoadToL0 / FixOutput 描蓝色（is-related），热路径边描黄色。
4. **硬件路径** → modal 显示 A5 微架构 SVG：GM（顶）/ AIC subsystem 左 / AIV subsystem 右。MTE2 节点描黄色 + 阴影发光，相关边（GM→MTE2→L1→MTE1→L0A/B→Cube）描金色。右侧 rail 列出 6 个 cycle-like counter 值，mte2 那行高亮为 dominant，并解释 "MTE2 (GM→L1)：Global Memory → L1 加载通路 busy 时长..."。
5. **Peer 视图** → 已经在 Mix 模式下，直接 scroll 到该 task 居中。
6. **下次采集** → 复制 `PROF_PMU_EVENT_TYPE=4` 到剪贴板，底部弹出 toast "下次采集：PROF_PMU_EVENT_TYPE=4（已复制）"。

## Scene 6 · 验证空状态（FR-03 / AC-07）

Lin 想看 Memory Access Flow，但本次 run 没采集 group 4。

- 点顶栏 `G4` tab → 切换。
- 左面板变为空状态卡：「**本次 run 未采集 group 4** — 一次运行只能采集一个 PMU group。若需要 group 4 的诊断，请重新运行并设置对应环境变量。」
- 下方 next-suggest 卡：`PROF_PMU_EVENT_TYPE=4` 〔复制〕
- 另一张 sum-card：「**当前已采集** Group 2 · Pipeline Balance · 切回 G2 即可继续诊断」+ 一键 "切到 G2" 按钮
- 中央泳道 fallback 为 identity coloring（即使 Mix=ON，因为无 group 2 数据可着色），title 显示 `Group · Wrap-grouped 顺序`。
- 右侧 Detail panel：「未选中 task」。

Lin 点 "切到 G2" → 回到 Scene 2 完整体验。

## Scene 7 · 横评（Mix 模式专属）

左侧 Top-3 Wraps 列表点击任意一行：

- W7 → 滚到 Wrap 7，详情切到 matmul_trans_k7（wait）
- W12 → 滚到 Wrap 12，详情切到 matmul_k12（wait，scalar_instr_busy 主导，给出下次建议 Group 6 · Conflict / Issue）
- W19 → 滚到 Wrap 19，详情切到 matmul_k19（overlap，cube_instr_busy 主导，给出下次建议 Group 1 · Cube Arithmetic）

每个 wrap 的 detail panel 都按其状态给出不同结论文本（wait / overlap / accounted 三套模板）。

## 验收对应 PRD

- AC-01（Group 切换）：✓ G1/G4/G5/G6/G7/G8 切到 disabled tab 后会进入空状态；G2 维持完整诊断。
- AC-02（泳道布局）：✓ AIC / AIV 都在对应 lane，不为单 task 拆 row；task 名称内嵌在 bar 内。
- AC-03（Group 2 gap 三态）：✓ wait / accounted / overlap 三色 badge + 三种结论文本；icache 计数在 event 区独立显示，不进 clc 求和。
- AC-04（Mix 模式）：✓ 同 Wrap 内 C/V 三 lane 紧邻，Peer 关系可视。
- AC-05（定位链路）：✓ 6 类 jump 全部有实质内容（4 个 modal + 1 个 scroll/mix + 1 个剪贴板复制）。
- AC-06（无效 counter）：✓ `0x0` 占位由数据生成器排除，UI 也只列举 `composition.length > 0` 的项。
- AC-07（未采集 group）：✓ 见 Scene 6。
- AC-08（预留入口）：✓ G1/4/5/6/7/8 tab 在顶栏明确显示为 disabled，hover 提示 "未采集"。
