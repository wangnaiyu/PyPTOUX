# UI Class Projection

本目录保存 PTO 稳定 UI class 的投影。

## Files

- `style.css`：来自 `.agents/skills/pto-design-system/css/style.css`

## Rule

- 可直接被 demo 或 `index.html` 引用。
- 不在这里手写新视觉语言；新组件先回 agent-facing skill 和 preview gate。
- module-local layout class 可以留在业务模块内，只有跨 demo 复用的视觉基础件才进入这里。
