# Themes

PTO 当前投影包含三类 theme surface：

- dark：默认工作台主题
- glass：实验性玻璃态 theme，需谨慎使用
- light：浅色兼容

语义 theme 定义见 `../tokens/semantic.css`。

## Rule

PyPTOUX demo 默认使用 dark theme。除非产品场景需要，不能把 glass theme 当作通用默认视觉语言。
