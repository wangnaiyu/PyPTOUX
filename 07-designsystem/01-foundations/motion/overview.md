# Motion

Motion token 见 `../tokens/foundation.css`：

- `--duration-fast`
- `--duration-base`
- `--duration-slow`
- `--easing-default`
- `--easing-out`

## Rule

PTO 动效用于状态反馈和空间理解，不用于装饰。graph、timeline、playback、split resize 等交互应优先遵循对应 pattern contract。
